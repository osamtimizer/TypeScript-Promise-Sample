# TypeScript-Promise-Sample
This repository contains some sample codes of Promise written in TypeScript.

## Usage

To build the sources, run the following commands.

```bash
$ npm install
$ npm run build
```

Then you can run the sample code.
```bash
$ node ./dist/Classes/SampleController.js
// --output--
// end line
// test1
```
